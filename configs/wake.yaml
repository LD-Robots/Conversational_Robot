engine: porcupine    # porcupine | asr
threshold: 60           # folosit doar pentru fallback-ul text (ASR)
wake_phrases:
  - "hello robot"

acknowledgement:
  ro: "Da, te ascult."
  en: "Yes, I'm listening."

# Bloc Porcupine păstrat doar ca fallback (nu se folosește când engine=openwakeword)
porcupine:
  access_key: "${PICOVOICE_ACCESS_KEY}"
  keyword_paths:
    - "/home/delia/Conversational_Robot/Conversational_Bot/voices/Hello-Robot_en_linux_v3_0_0.ppn"
  keywords:
    - "porcupine"
  sensitivities: [0.6]