engine: openwakeword

acknowledgement:
  en: "Yes, I'm listening."
  ro: "Da, te ascult."

wake_phrases:
  - "hello robot"
  - "hey robot"
  - "what's up buddy"
  - "bună robot"
  - "salut robot"
  - "listen up"

threshold: 65

openwakeword:
  melspec_model_path: "voices/melspectrogram.onnx"
  embedding_model_path: "voices/embedding_model.onnx"
  wake_keyword: hello_robot
  keywords:
    hello_robot:
      model_path: "voices/hello_robot.onnx"
      threshold: 0.5
      cooldown_ms: 2000
    
    wots_up_bud_dee:
      model_path: "voices/Wots_up__bud_dee.onnx"
      threshold: 0.5
      cooldown_ms: 2000
      
    boona_ro_bot:
      model_path: "voices/boona__ro_bot.onnx"
      threshold: 0.5
      cooldown_ms: 2000

    sa_loot_ro_bot:
      model_path: "voices/sa_loot_ro_bot.onnx"
      threshold: 0.5
      cooldown_ms: 2000
    
    lis_un_up:
      model_path: "voices/lis_un_up.onnx"
      threshold: 0.5
      cooldown_ms: 2000

