engine: openwakeword
wake_phrases: ["hello robot"]
threshold: 72
acknowledgement:
  en: "Yes, I'm listening."
  ro: "Da, te ascult."

openwakeword:
  keywords:
    hello_robot:
      model_path: "voices/hello_robot.onnx"
      threshold: 0.3
      cooldown_ms: 1200
      kind: wake
  inference_framework: onnx
  sample_rate: 16000
  block_ms: 20